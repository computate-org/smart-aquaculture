apiVersion: batch/v1
kind: Job
metadata:
  name: local-setup
  namespace: smartaquaculture
spec:
  backoffLimit: 1000
  template:
    spec:
      restartPolicy: OnFailure
      serviceAccountName: local-setup
      volumes:
        - name: local-setup-playbook
          configMap:
            name: local-setup-playbook
        - name: local-setup-defaults
          configMap:
            name: local-setup-defaults
        - name: local-setup-templates
          configMap:
            name: local-setup-templates
        - name: local-setup-tasks
          configMap:
            name: local-setup-tasks
        - name: home
          emptyDir: {}
        - name: ansible-tmp
          emptyDir: {}
      containers:
        - name: local-setup
          image: quay.io/nerc-images/keycloak-ansible:ansible-collection-upgrade
          imagePullPolicy: Always
          volumeMounts:
            - name: local-setup-playbook
              mountPath: /home/ansible/.ansible/playbooks/
            - name: local-setup-tasks
              mountPath: /home/ansible/.ansible/roles/local-setup/tasks/
            - name: local-setup-defaults
              mountPath: /home/ansible/.ansible/roles/local-setup/defaults/
            - name: local-setup-templates
              mountPath: /home/ansible/.ansible/roles/local-setup/templates/
            - name: home
              mountPath: /home/ansible
            - name: ansible-tmp
              mountPath: /home/ansible/.ansible/tmp/
          env:
            - name: ANSIBLE_ROLES_PATH
              value: /home/ansible/.ansible/roles
            - name: POD_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
          command: ["/bin/bash", "-c", "--"]
          args:
            - |
                ansible-playbook /home/ansible/.ansible/playbooks/local-setup.yaml
